import { Router, Request, Response } from "express";

export default(): Router =>{
  const router = Router();
  router.post("/crear", function(req: Request, res: Response){

  })





  return router;
};